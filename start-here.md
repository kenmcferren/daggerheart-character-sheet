# Start Here

Read this first every session. Update as steps finish.

## Project
Daggerheart guided character creation/level-up website. Output: printable PDF. Local saves. Source-pack architecture so third-party sources load the same way as the two primary books. Details: `Docs/plan.md`.

## Current series
- Series 1, 2 and 3 archived. Series 4: PDF output (not started).

## Current step
- Series 4, step 1: sheet layout design (owner approves). Not started.

## Done
- Series 3, steps 4-8 (UI): `src/App.tsx` shell; `src/ui/Start.tsx` (frame + supplements, conflicts block Start, saved list, JSON import/delete), `Wizard.tsx` (step nav with free jump back, progress bar, per-step validation, Next blocked until the step is valid), `steps.tsx` (all base steps, Beastbound companion, Martial Artist stances, Mixed Ancestry, Iconic Weapon builder, frame text steps), `Review.tsx` (summary, jump links, starting numbers, JSON export), `data.ts` (packs, IndexedDB store). Autosave 400 ms after each change. `tests/ui-util.test.ts`. Driven live in the browser pane: conflicts, Ranger/Beastbound + Tech + Floating Magic School path to Review, autosave, reload, reopen.
- Series 3, step 3: `scripts/srd-convert/class-extras.mjs` (called by `build-core-pack.mjs`) adds structured `beastforms` (24), `companion`, `stances` (16) + `focusMax`, `uses`/`usesPer`/one-circle `trackers` on the 65 once-per-rest cards (and class features: uses only), and a `multiclass` levelUpOption (min level 5). Schema extended (`extend-schema-s3.cjs`). `tests/class-extras.test.ts` (6, `[s3]`).
- Series 3, step 2: `src/engine/rules.ts` (`validateCreation`, `deriveStats`, `weaponPool`/`armorPool`, `downtimeMoves`, `ancestryFeatures`); `Character.creation` block (optional, defaulted by `creationOf`). SRD trait array is +2,+1,+1,+0,+0,-1 (plan text corrected). `tests/rules.test.ts` (17, `[s3]`).
- Series 3, step 1: `src/engine/creation.ts` `buildCreation(packs, {frame, supplements})` returns ordered wizard steps (SRD order; `insert-step` before a step or `creation` = ahead of all), annotations by target, item pool contributions, and conflicts (pool replaced twice, replace+add, resource set twice, duplicate step/tracker, move add+remove). `tests/creation.test.ts` (10 tests, `[s3]`). Not committed.
- Series 2, step 4: `node scripts/validate-packs.mjs` (or `npm run validate-packs`) checks schema, duplicate ids, class/subclass/domain/card cross-references, 21 cards per domain, frame annotate targets and pool refs, empty rules text. Writes `TestArtifacts/pack-report/report.md`. Currently 0 problems; 1 note (Everyday Hero and Tech both replace the weapons pool: wizard must flag). `tests/pack-report.test.ts` (2 tests, `[s2]`, includes a broken-pack fixture).
- Series 2, step 3: `packs/core/srd-frames.json` (extends srd-core), generated by `scripts/srd-convert/build-frame-pack.mjs`. 9 `campaignFrames` entries: Witherwild (`frameKind: frame`; annotate ops for classes/ancestries/communities with questions, Wither tracker, back-sheet rules, session-zero questions) and 8 supplements (Everyday Hero, Western, Monster Hunting, Tech, Feasts, Fairy Tale, Floating Magic School, Grimdark). Item pools live inside each entry (`pools`), referenced by ops. `tests/frame-pack.test.ts` (6 tests, `[s2]`). Not committed.
- Series 2, step 2: kind-specific schema (class, subclass, ancestry, community, domain, domainCard, equipmentItem, campaignFrame, plus tracker/choice/frameOp/count definitions). `scripts/srd-convert/build-core-pack.mjs` generates `packs/core/srd-core.json` from the SRD Markdown (13 classes, 26 subclasses, 24 ancestries, 15 communities, 10 domains, 210 cards, 614 equipment rows). `tests/core-pack.test.ts` (5 tests, `[s2]`) checks schema, counts, cross-references. Not committed.
- Series 2, step 1: `Daggerheart SRD.pdf` split into 469 cross-linked Markdown files in `Daggerheart SRD Files/` (entry point `SRD Index.md`; provenance and SRD oddities in `Conversion Notes.md`). Conversion scripts in `scripts/srd-convert/` (README there). Not committed.
- Step 6: invented placeholder packs `packs/sample/sample-core.json` + `sample-extension.json` (extends + override); `tests/sample-packs.test.ts` loads them and resolves a character's ids against the registry. Logged in `Docs/deferred-queue.md`.
- Step 5: `src/engine/character.ts` (model, validator, JSON export/import), `saves.ts` (`CharacterStore` over a `KeyValueStore`), `idbStore.ts` (idb-keyval adapter). 12 tests in `tests/character.test.ts` tagged `[s1]`. Characters store only pack entry ids. Schema version 1; no migrations yet.
- Step 4: `schema/source-pack.schema.json` (ajv-validated), `src/engine/packs.ts` (`validatePack`, `loadPacks`: extends ordering, duplicate-id errors, id-preserving overrides), 13 tests in `tests/packs.test.ts` tagged `[s1]`. Per-kind entry fields (beyond id/name/description) are deliberately open until the SRD shape is known in Series 2.
- Step 3: harness. `Run-All-Tests.bat` (or `node scripts/run-all-tests.mjs`) syncs `TestPolicy/test-master-list.csv`, runs vitest, writes `TestArtifacts/<run-id>/report.html`. Governance = top-level `describe` name must start with `[evergreen]` or `[sN]`; sync fails otherwise. Flags applied by `tests/setup.ts` (0 = skip). `npm test` also honors the list.
- Step 2: Vite/React/TS scaffold, vitest (`npm test`), oxlint (`npm run lint`), folders `src/engine`, `src/ui`, `schema`, `packs`, `tests`. Build, smoke test, lint pass.
- Collaboration scaffolding (CLAUDE.md, backlog, plan, deferred queue, .gitignore, TestPolicy list, memory).
- Series 1-6 drafted in `Docs/plan.md`.

## Unverified
- UI: verified by driving it in the browser pane (one path). Not tried: other classes' flows, dark mode, phone width, print. IndexedDB works (autosave, reload, reopen confirmed). Owner has not looked at the visuals; styling is a plain placeholder.
- Rules gaps: armor features that change numbers (e.g. "+1 to Evasion") are shown as text only, not added into Evasion/thresholds. Ranger companion Stress slots are not in the SRD text I parsed, so not modelled. Legendary/Mythic Beast entries are upgrade rules (no stat line) and keep their text as features. "Once per rest" detection is a text match on rules; the 65 cards were counted, not read one by one. Mixed Ancestry, Beastbound and Martial Artist paths are covered by engine tests, Beastbound also by the live run.
- Creation engine: Tech's replace-pool points at a builder choice, so its pool has no entries (the builder is the pool; wizard must handle). Conflicts are reported, not resolved: the start screen (step 4) decides how to block or warn. Frame ops other than pools/steps/annotate are passed through in `ops` for later steps.
- Frame pack judgment calls (owner confirmed 12 Wither boxes, 6 scars, Prepare removed, Tech slots): Wither tracker is 12 boxes (Fear Die is a d12; SRD gives no per-PC count); Grimdark scars tracker is 6 boxes (Hope slots); Feasts removes Tend to Wounds, Clear Stress and Prepare (owner: Feast replaces gaining Hope; note Prepare also refreshes per-rest uses, which the wizard/back sheet must still cover); Tech Upgrade slots are 2 + 1 per tier (`perTier`). Colossal, Hex Crawl and Faction Tracking are GM-only and left out. Conflict detection between supplements is not implemented (wizard work).
- Core pack: generated by script, checked by counts/refs only. Rules text is verbatim SRD but not compared line by line. Mixed Ancestry (choice type `pick-one-each-from`) and Multiclassing are not yet modelled. Ranger companion, Druid beastforms and Brawler stances sit in a class `supplements` list as raw rules text, not structured.
- `idbStore.ts` (real IndexedDB) untested until a UI exists.
- Harness verified from the CLI (run, skip, ungoverned failure); browser auto-open fixed (path escape bug) and confirmed by owner opening the report.
- SRD Markdown files: checked by counts and link checks, not read line by line. Pages checked against images: 101, 102 (hand-transcribed), 193, 194, 196. Details in `Daggerheart SRD Files/Conversion Notes.md`.

## Very next action
- Series 4, step 1: propose the sheet layout for owner approval. Owner may still try `npm run dev` and report UI issues.
