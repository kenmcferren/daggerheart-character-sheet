# Start Here

Read this first every session. Update as steps finish.

## Project
Daggerheart guided character creation/level-up website. Output: printable PDF. Local saves. Source-pack architecture so third-party sources load the same way as the two primary books. Details: `Docs/plan.md`.

## Current series
- Series 1-5 archived. UI touch-up pass and saved-versions restyle done 2026-09-23 (owner-approved; archived in long-term-done.md). Series 7: step 1 done; step 2 (condense text for the 4-page level 10 target) 2a-2d all drafted 2026-09-23, owner reviewed at first glance 2026-09-23, all fine. Series 8: Druid forms page, done. Series 6 still open, untouched.

## Current step
- Series 7, step 1 in progress. Listing passes done and reviewed (owner 2026-09-22): `TestArtifacts/rules-math/candidates.md` (52 number-changing hits), `trait-candidates.md` (40 trait-sizing hits), sorted in `proposal.md` into Group A (permanent unconditional -> bake into the sheet), Group B (build-conditional: gear/loadout) and Group C (situational: in-play conditions).
- Group A done: `Rugged`, `Unwavering`/`Unrelenting`/`Undaunted` (owner: stack), `Fleeting Shadow`, `Ascendant`, `Stoneskin`, `Nimble` baked into Evasion/thresholds/Armor Score (`Feature.statBonus`, `scripts/srd-convert/stat-notes.mjs` `STAT_BONUSES`, summed in `sheetView.ts`).
- Footnote mechanism built for Group B/C (owner design): † (build, e.g. Armorer) and ‡ (situational, e.g. Scorpion's Poise) symbols next to the affected stat label and next to the feature/card/item name, with a legend line at the bottom of page 1 (front-page notes) and after the last domain card (card-page notes). Data: `Feature.statNote`/`domainCard.statNote`/`equipmentItem.statNote` (schema `statNote` def), `stat-notes.mjs` `STAT_NOTES`, `sheetView().statNotes`, `Sheet.noteSym`/`Sheet.legend` in `sheet.ts`.
- First representative batch applied (one of each source/kind, to confirm the design before the full sweep): Armorer, Fortified Armor, 4 Mage Robes tiers, Granminster's Finery (†); Scorpion's Poise, Battle-Bonded, Elusive Predator, Shadowhunter, Vial of Darksmoke (‡). Samples sent: `guardian-stalwart-armorer-statnotes.pdf`, `assassin-executioners-guild-statnotes.pdf`. Owner approved 2026-09-22: symbol placement, legend wording/position, †/‡ split all confirmed as-is.
- Full sweep done (2026-09-22): re-read all 52 + 40 candidates in `candidates.md`/`trait-candidates.md` against the confirmed pattern (always-on conditional stat bonus, no per-use cost). Owner confirmed scope: costed/consumable abilities (mark a Stress, spend Hope, drink a potion — e.g. Wings, Eye of the Storm, Potion of Vigilance) stay plain text, no footnote; the card/feature text already says the whole effect. Only one new match: `Eldritch Flesh` (dread domain card, +1 both thresholds per Stress marked) — added to `stat-notes.mjs` `STAT_NOTES.domainCard` (‡). Everything else in both lists was already covered (baked into Group A, already a resource tracker, a normal weapon/spell roll using a trait, or a costed/temporary effect that's Group C by the confirmed scope). 13 stat notes + 8 stat bonuses now applied at build. `tests/stat-notes.test.ts` still 4/4; full suite 38 passed, 0 failed, 151 skipped.
- Not yet decided: Vitality (one-time permanent choice among 3 options) has no mechanism to record the choice, left out of this pass. The "-Touched" cards (Blade/Splendor/Valor) depend on loadout vs. vault, which isn't modelled; flagged in `proposal.md`, not marked yet.
- Tests: `tests/stat-notes.test.ts` (4, `[s7]`). Full suite 38 passed, 0 failed (151 skipped by policy).

## Done
- Series 7, step 2 (2026-09-23, drafted, owner review pending except ancestries): condensed wording as hand-edited JSON in `scripts/srd-convert/short-heritage/`: `ancestries.json`, `communities.json` (`applyHeritageShort`), `campaign-rules.json` (applied in `build-frame-pack.mjs`, entries keyed by back-sheet title / `move:<name>` / `grant:<item>`, optional `table`), `domains/<domain>.json` (`applyDomainShort`, all 10 domains, 210 cards; a value is a string or `{short, table}`). Words: ancestries 967->691, communities 409->282, campaign rules 1732->665, domain cards 11487->9757 (15%). Frame annotations (Witherwild class/ancestry/community notes) no longer print (creator-only; owner). New print features: `Sheet.zebra` table (bold header, grey/white rows, columns sized to widest word; cell `plain` option so words are not split), card tables (Teleport, Forager, Tempest, Bare Bones), and character-value substitution on cards: `{{key|template|fallback}}` in a card short (`substitute` in `sheet.ts`; template `#` = the value, printed bold BLUE (`BLUE`, only non-gray colour on the sheet; print tests exempt exactly that colour), fallback = SRD wording when the character lacks the value). Keys: spellcast, agility, agilityMin1, strength, strength2, strengthPlus3, knowledgeMin1, presence, level, proficiency, halfProfUp (owner: round half Proficiency up), halfAgilityUp, finesseOrAgility (higher of the two; assumption, card says "either"). Tests: `tests/short-text.test.ts` (shorts no longer than SRD, no new numbers, once-per wording kept, grimoire spell lines kept, marker keys known), `tests/sheet.test.ts` `[s7]` blocks. Pattern audit after 2d: 148 sheets at 3 pages, 44 at 4, 16 at 6 (all Druid, the Beastform pages); 0 problems. Not looked at by owner: rendered card/rules pages.
- Series 5, step 6 (owner change): multiclass extras. Stances and the companion follow the subclass that holds them, main or multiclass (`extrasOf`, `STANCE_SUBCLASS`, `COMPANION_SUBCLASS` in `levelup.ts`). Multiclass into Brawler / Martial Artist: the level record carries `startStanceIds` (two Tier 1 stances), then one stance per level; into Ranger / Beastbound: `newCompanion` (name, 2 Experiences, attack, damage type), then one option per level. Judgment call: the level a multiclass takes the subclass, the starting choices replace that level's per-level pick. Behavior change (was a bug): a Juggernaut Brawler is no longer asked for stances and a Wayfinder Ranger is no longer asked for companion options. `Progress.companion` (from creation or the record), `sheetView().stances`; level-up UI class step handles both. Combo Die stays text only (owner: nothing to track); Druid beastform definitions stay on the long-term list. Tests `tests/multiclass-extras.test.ts` (5, `[s5]`); test helpers now make Brawler Martial Artist. All 99 run tests pass; pattern audit 3 pages 121, 4 pages 87. Owner previewed the level-up UI live with a new character (multiclass class step included) and confirmed the sheets look right, including the sample PDFs.
- Series 5, step 6 (owner change): once-per-use circles for class, subclass, hope, heritage, multiclass and companion feature text. `Sheet.withUses` (`sheet.ts`) puts an inline fill-in circle after "once per rest/long rest/short rest/session/scene", and one per use for "twice" / "two, three, four times per ..." (extra circles follow as their own marks); same look as the domain card circles, no tracker-section entry. Talisman tracker stays at 6. Test in `tests/resource-trackers.test.ts`. Not looked at by anyone; long rules text near a circle may wrap differently.
- Series 5, step 6 (owner change): resource trackers. Counters with an upper limit and no die print as rows of boxes under the Hope / Hit Points / Stress / Armor bands: Warlock Favor (6), Witch Hex (Spellcast trait), Assassin Toxic Concoctions tokens (5; Poisoners Guild), Witch Hedge Enchanted Talisman (6, = Hope slots), Walk Between Worlds and Circle of Power tokens (Spellcast trait). Data: `scripts/srd-convert/resource-trackers.mjs` (table applied by `build-core-pack.mjs`) -> `feature.trackers` -> `sheetView().resourceTrackers` -> `sheet.ts` extra trackers. Held features only (subclass rank, multiclass module included). Not tracked (judgment): dice pools (Prayer, Slayer, Rally, Patron dice), once per rest/session uses, Marked for Death and Charged (states). Test `tests/resource-trackers.test.ts`; samples `TestArtifacts/sheet-samples/resource-trackers-*.pdf` not looked at.
- Series 5, step 6 (owner change): Ranger companion (Beastbound) prints as a three-column block at the top of the domain card sheet (`sheetView().companion` -> `Sheet` in `sheet.ts`; header says "Companion; Domains: ..."; no longer on page 1). Col 1: name (the player can put the creature in it), Evasion box (10, +2 Aware), Stress boxes (3, +1 Resilient), Attack (+Spellcast trait bonus, attack text), Range, Attack (+Spellcast trait bonus from the main sheet, Agility for Rangers) and Damage (Proficiency + die, type, attack text). Vicious: level-up asks die or range (`LevelRecord.viciousChoice`, `Progress.companionVicious`), and column 1 shows the raised value. Col 2: companion Experiences with bonuses. Col 3 (only if taken): Creature Comfort, Armored, Bonded. Intelligent, Vicious, Resilient, Aware are folded into the numbers; Light in the Dark prints as an extra Hope box on page 1 beside the Hope label. Level-up: choosing Intelligent now also asks which companion Experience gets +1 (`LevelRecord.companionExperience`, `Progress.companionExperienceBonus`; only required when the character has a companion). Sample `TestArtifacts/sheet-samples/ranger-beastbound-level-5-companion.pdf`; not looked at by anyone. Companion base Stress of 3 confirmed by owner (read off the printed sheet).
- Series 5, step 6 (owner change): Druid elements (Warden of the Elements) print as a full-width table across the top of the domain card sheet: element rows (Fire, Earth, Water, Air) on the left, one column per held Elemental feature (Incarnation; Aura from specialization; Dominion from mastery). Mechanism: a feature in `short/<class>.json` may carry `"table": {"title", "rows": [{name, short}]}` -> `feature.sheetTable` -> `sheetView().sheetTable` -> `Sheet.table` (one item, never split; cards start below it). The three feature shorts now point to the table. Sample `TestArtifacts/sheet-samples/druid-level-5-elements.pdf` sent to owner; not yet reviewed.
- Series 5, step 6 (owner change): Assassin poisons print on the domain card sheet after any stances and before the cards, one block each (label "Poison"), only those known at the subclass rank. Mechanism: a feature entry in `short/<class>.json` may carry `"cards": {"label", "items": [{name, short}]}` -> `feature.sheetCards` -> `sheetView().sheetCards` -> blocks in `sheet.ts`; each item name must appear in the SRD text (build throws otherwise). Toxic Concoctions / Poison Compendium / Venomancer shorts now point to the domain card sheet.
- Series 5, step 6 (condensed wording, batch 2 + owner changes): Bard, Brawler (with all 16 stances), Druid, Guardian drafted (files in `scripts/srd-convert/short/`), awaiting owner approval. Brawler stances now print on the domain card sheet before any card, only chosen ones, one block each (header "Martial Stances; Domains: ..."); removed from page 1. Druid Beastform text says "see SRD for the forms"; Druid forms page is on the long-term list. Assassin approved.
- Series 5, step 6 (condensed wording, infrastructure): owner decisions: SRD text stays in `rules`, condensed text sits beside it as `short` (sheet prints `short`, Creator shows `rules`); all 13 classes in SRD order; also stances and companion options (Druid beastforms are not printed on the sheet yet, so they wait until they are). Hand-edited files `scripts/srd-convert/short/<class>.json` (arrays aligned to features by position with the name checked; `""` = folded into the previous feature) are merged by `build-core-pack.mjs` (`short-text.mjs`). Sheet uses `f.short ?? f.rules` (`Sheet.features`, hope, stances, companion options, multiclass module). `node scripts/short-report.mjs` writes words saved per class (`TestArtifacts/short-report/report.md`) and side-by-side review files (`TestArtifacts/short-review/<class>.md`). `tests/short-text.test.ts` (`[s5]`): each short is no longer than the SRD text it replaces and adds no numbers. Assassin done: 425 -> 273 words (36%).
- Series 5, step 6 (owner layout change, round 1): Inventory / Notes moved from page 1 to the top of page 2; rules are anchored to the bottom of page 2 under a rule line (Rest Rules open column 1; campaign rule sections stay whole and are split so the three columns end about even; too many campaign rules to fit one page fall back to pouring under the inventory). Domain cards are never split across columns or pages (`Sheet.pourBlocks`). Audit now records front + blank + card pages (`lastLayout` in `sheet.ts`). Result (208 sheets, level 10): 3 pages 99, 4 pages 63, 5 pages 39, 6 pages 7. Front sheet is 2 pages for 172 sheets; 36 need 3 (then a blank page pads so cards start a new sheet): all Brawler paths, Druid, and multiclass paths. Card pages: 1 page holds about 10 cards, so 11-14 cards need 2 pages in 63 sheets. Owner target at project end: level 10 <= 4 pages, reached by condensing class/subclass/rules text (Series 7 work).
- Series 5, step 5: leveling-pattern audit. `tests/lib/patterns.ts` builds legal paths (8 named patterns + 6 seeded random, worst-case longest domain-card text) and `tests/level-patterns.test.ts` runs 13 classes x 14 paths to level 10 (multiclass-at-5 also at 5 and 8) = 208 sheets: every path validates, replays deterministically, stays inside the margins in grayscale. Result: 3 pages (95 sheets), 4 (31), 5 (77), 6 (5); level 1 sheets are 3 pages. Longest: multiclass + all subclass cards + 11-14 cards (Ranger, Wizard, Bard multiclass; Bard seed 2; Druid seed 3). Page budget set to 6; the test fails if a sheet gets longer. Report `TestArtifacts/pattern-audit/report.md`, seeds in `run.json`, 6-page sheets saved as PDFs beside them. The matrix takes about 3.5 minutes (flip to 0 in the policy file after archiving). Engine fix found by the generator: a Ranger has 8 companion options, each once, so from the level they run out the requirement is min(wanted, left).
- Series 5, step 4 fix (owner): Hit Points, Stress and Armor Slots are capped at 12. The engine refuses (and the level-up screen greys out) Hit Point / Stress advancements that would pass 12 (`SLOT_CAP`, `Progress.room`); `sheetView` clamps too; the sheet is back to two rows of 6. A 7 Hit Point class can take five Hit Point slots.
- Series 5, step 4: sheet at level N. `src/engine/sheetView.ts` (`sheetView(ch, setup)`: base stats + replayed history: Evasion/HP/Stress/Proficiency, trait totals, Experiences with bonuses, all cards held, subclass cards taken (main, then multiclass subclass), multiclass module = the added class's features, Spellcast traits, stances, companion options, Brawler Combo Die). `renderSheet` reads it: header shows Multiclass, Experience lines show real bonuses, HP/Stress/Armor bands grow past 12 rows-wise (Guardian 7 + 6 = 13), left column lists every subclass card held, right column adds Combo Die, Multiclass class features, companion options; back of sheet lists every card held. With two Spellcast traits both are starred and spellcast weapons use the higher. **Make PDF** button on the level 2+ character screen (file name ends `-level-N`). `tests/sheet-levelup.test.ts` (8, `[s5]`), shared helpers `tests/lib/levelup-paths.ts` and `tests/lib/record.ts` (draw-call recorder moved out of `sheet-print.test.ts`). 141 tests pass with all flags on.
- Series 5, step 3 (owner feedback round 1): every dropdown in the level-up screens is now a button grid with a light details dropdown (`PickGrid`, `CardPicker` in `src/ui/common.tsx`): domain cards as one section per domain (class domains first, then the multiclass domain), swap out / swap in, stances, companion options, tier box, subclass, multiclass class / domain / subclass / cross-out tier. Traits and Experience choices stay checkboxes. Saved-version tree now shows, for versions of the same level, each version's value for Subclass, Domain cards, Experiences and Multiclass, only for the areas that differ (`src/engine/branches.ts`, tests in `levelup.test.ts`). Checked live: two level 2 branches showed different Domain cards and Experiences and nothing else. Seeded test character removed again. No PDF button on level 2+ screens is deliberate: the sheet still reads level 1 data (step 4).
- Series 5, step 3: guided level-up UI. Saved list is one entry per character (Level up button, "N versions" tree with Open / Level up from here / Delete per version, Delete removes all versions). `src/ui/LevelUp.tsx`: steps Tier achievement (Experience text) -> Advancements (options greyed with the reason, tier box chooser, per-option detail forms for traits, Experiences, domain card, subclass card, multiclass incl. which tier's subclass box to cross out) -> Domain cards (new card + optional exchange) -> class extras (Brawler stance, Ranger companion options) -> Review; Confirm saves a NEW version. `src/ui/CharacterView.tsx` (level 2+): level-by-level log, versions, export version JSON, Level up. `src/ui/describe.ts`, `Versions.tsx`. Engine additions: `optionAvailability`, `afterAdvancements`, `levelUpStages`, `domainCardProblem`, `companionOptionCount`. Multiclass/subclass rule change (owner): multiclass crosses out this tier's or next tier's subclass box (player chooses); a subclass card may still be taken later in a tier where Multiclass was taken. Driven live through DOM scripts: migrated v1 Ranger -> level-up screens incl. companion step; seeded Wizard level 1 -> 2, then a branch (Level up from here on level 1) gave two separate level 2 versions. Seeded test character removed from IndexedDB afterwards. 132 tests pass with all flags on.
- Series 5, step 2: history and versions. `Character` schema v2: `lineageId` (shared by all versions), `parentId`, `history: LevelRecord[]` (one per level after 1: new Experience, advancements with details, new card, optional swap, stance / companion options); v1 saves migrate on load (level 1, own lineage, `levelUpChoices` dropped). `src/engine/levelup.ts`: `replay`/`progressOf` (derives proficiency, Evasion/HP/Stress bonuses, trait bonuses + marks, Experiences with bonuses, cards held, subclass ranks, multiclass, slots used/crossed, combo die, stances, companion options), `validateLevelUp` (every rule, human reasons), `applyLevelUp` (returns a NEW version; the old one is untouched, so re-leveling an old version makes a branch). `CharacterStore` gained `versionsOf`, `lineages`, `removeLineage`. `tests/levelup.test.ts` (32, `[s5]`). All 132 tests pass with every flag set to 1 (policy restored).
- Series 5, step 1: level-up rules as pack data. `scripts/srd-convert/level-up-rules.mjs` (called by `build-core-pack.mjs`) writes 17 `levelUpOptions` into `srd-core`: 3 tier achievements (levels 2/5/8), 9 advancements with slots per tier, cost, minLevel, trait marking and cross-outs (Multiclass and Upgraded subclass), 2 fixed steps (thresholds, new card), 3 class-specific (Brawler combo die advancement, Brawler stance per level, Ranger companion option per level). Schema: `levelUpOption` definition (`extend-schema-s5.cjs`). `tests/levelup-rules.test.ts` (6, `[s5]`). Slot counts come from the printed level-up sheet the owner supplied, not the SRD text.
- Series 4, step 5 (final layout, note 2): page 2 (back of the sheet) = rest rules then campaign rules poured through three columns; domain cards (with name and Domains subtext) start their own sheet, padded with a blank page so they begin on an odd page number when the rules run past page 2. Known ugly: markdown tables in campaign rules (e.g. Tech Scrap table) print as raw `|` text.
- Series 4, step 5 (final layout, note 1): the class-hope/subclass and class-features columns now get widths that make the band as short as possible and the two columns about the same height (`buildLeft`/`buildRight` search in `sheet.ts`, min 140 pt each). Render is slower (about 0.4 s per sheet); heavy sheet tests have a 60 s timeout.
- Series 4, step 4 (print checks, automated): `tests/sheet-print.test.ts` (4, `[s4]`) records every draw call and asserts all marks stay inside the 0.5" margins and every colour is gray/black, for all 13 classes, all supplements, 14 cards, and oversized text (long name, long experiences, unbroken long word, 40-line notes). Fixes: names/lines shrink then truncate with "..." (`fitText`), over-wide words are split (`breakWord`). Not checked: a real printer.
- Series 4, step 3 (refinement round 2): white-filled boxes in gray areas; trait numbers raised; gold as fill-in circles (10 handfuls, 10 bags, 1 chest; Tech keeps a Credits box); grimoire spells bold, one per line (pack fix in `build-core-pack.mjs`: Illiat, Vagras, Exota); "once per rest" circle inline in card text (row fallback if the phrase is absent); rest rules pruned to the owner's wording, third column, bottom right (campaign rules join it if under 300 pt, else page 3).
- Series 4, step 3 (refinement round 1): sheet rebuilt to the owner's mockup. Page 1: name rule; defense stats + Hope/HP/Stress/Armor marks (dark = earned, faint = room to the cap of 12, gray bands on HP and Armor rows) + extra trackers | Heritage | Traits (spellcast trait bold with *); Armor | Primary weapon (gray) | Secondary weapon (Tech: Iconic Weapon is weapon 1, third cell = Upgrades + slot boxes); Experiences (6 lines); Class Hope + Subclass (1/3) | Class Features + companion/stances (2/3); Inventory / Notes with gold box on the header row and a Kit line. Page 2: domain cards (3 columns, checkbox each = loadout), rest rules (SRD verbatim, `src/pdf/restRules.ts`) + campaign rules two columns wide at bottom right; if they are over 520 pt tall they start their own page. Weapon damage gets Proficiency dice in front only when Proficiency > 1 (`damageText`). Italic fonts added.
- Series 4, step 2: `src/pdf/sheet.ts` (`renderSheet(character, setup, fonts)` -> PDF bytes; Letter, 2 pages plus continuation pages on overflow; columns flow item by item), `src/pdf/browser.ts` (lazy-loaded, fetches fonts, downloads), Review's **Make PDF** button. Fonts: Source Sans 3 + Cinzel in `assets/fonts/`. `tests/sheet.test.ts` (5, `[s4]`) renders all 13 classes, an empty character, long notes (4 pages), and Witherwild+Tech+Grimdark; sample PDFs land in `TestArtifacts/sheet-samples/`.
- Frame choice removed from the UI: Witherwild is now `frameKind: supplement` and listed with the other campaign options. Engine still supports a frame slot; old saves with `campaignFrameId` are treated as a supplement (`setupOf`). Session-zero questions show for any selected option that has them. All 82 tests pass with every flag temporarily set to 1 (policy file restored).
- Series 3, steps 4-8 (UI): `src/App.tsx` shell; `src/ui/Start.tsx` (frame + supplements, conflicts block Start, saved list, JSON import/delete), `Wizard.tsx` (step nav with free jump back, progress bar, per-step validation, Next blocked until the step is valid), `steps.tsx` (all base steps, Beastbound companion, Martial Artist stances, Mixed Ancestry, Iconic Weapon builder, frame text steps), `Review.tsx` (summary, jump links, starting numbers, JSON export), `data.ts` (packs, IndexedDB store). Autosave 400 ms after each change. `tests/ui-util.test.ts`. Driven live in the browser pane: conflicts, Ranger/Beastbound + Tech + Floating Magic School path to Review, autosave, reload, reopen.
- Series 3, step 3: `scripts/srd-convert/class-extras.mjs` (called by `build-core-pack.mjs`) adds structured `beastforms` (24), `companion`, `stances` (16) + `focusMax`, `uses`/`usesPer`/one-circle `trackers` on the 65 once-per-rest cards (and class features: uses only), and a `multiclass` levelUpOption (min level 5). Schema extended (`extend-schema-s3.cjs`). `tests/class-extras.test.ts` (6, `[s3]`).
- Series 3, step 2: `src/engine/rules.ts` (`validateCreation`, `deriveStats`, `weaponPool`/`armorPool`, `downtimeMoves`, `ancestryFeatures`); `Character.creation` block (optional, defaulted by `creationOf`). SRD trait array is +2,+1,+1,+0,+0,-1 (plan text corrected). `tests/rules.test.ts` (17, `[s3]`).
- Series 3, step 1: `src/engine/creation.ts` `buildCreation(packs, {frame, supplements})` returns ordered wizard steps (SRD order; `insert-step` before a step or `creation` = ahead of all), annotations by target, item pool contributions, and conflicts (pool replaced twice, replace+add, resource set twice, duplicate step/tracker, move add+remove). `tests/creation.test.ts` (10 tests, `[s3]`). Not committed.
- Series 2, step 4: `node scripts/validate-packs.mjs` (or `npm run validate-packs`) checks schema, duplicate ids, class/subclass/domain/card cross-references, 21 cards per domain, frame annotate targets and pool refs, empty rules text. Writes `TestArtifacts/pack-report/report.md`. Currently 0 problems; 1 note (Everyday Hero and Tech both replace the weapons pool: wizard must flag). `tests/pack-report.test.ts` (2 tests, `[s2]`, includes a broken-pack fixture).
- Series 2, step 3: `packs/core/srd-frames.json` (extends srd-core), generated by `scripts/srd-convert/build-frame-pack.mjs`. 9 `campaignFrames` entries: Witherwild (`frameKind: frame`; annotate ops for classes/ancestries/communities with questions, Wither tracker, back-sheet rules, session-zero questions) and 8 supplements (Everyday Hero, Western, Monster Hunting, Tech, Feasts, Fairy Tale, Floating Magic School, Grimdark). Item pools live inside each entry (`pools`), referenced by ops. `tests/frame-pack.test.ts` (6 tests, `[s2]`). Not committed.
- Series 2, step 2: kind-specific schema (class, subclass, ancestry, community, domain, domainCard, equipmentItem, campaignFrame, plus tracker/choice/frameOp/count definitions). `scripts/srd-convert/build-core-pack.mjs` generates `packs/core/srd-core.json` from the SRD Markdown (13 classes, 26 subclasses, 24 ancestries, 15 communities, 10 domains, 210 cards, 614 equipment rows). `tests/core-pack.test.ts` (5 tests, `[s2]`) checks schema, counts, cross-references. Not committed.
- Series 2, step 1: `Daggerheart SRD.pdf` split into 469 cross-linked Markdown files in `Daggerheart SRD Files/` (entry point `SRD Index.md`; provenance and SRD oddities in `Conversion Notes.md`). Conversion scripts in `scripts/srd-convert/` (README there). Not committed.
- Step 6: invented placeholder packs `packs/sample/sample-core.json` + `sample-extension.json` (extends + override); `tests/sample-packs.test.ts` loads them and resolves a character's ids against the registry. Logged in `Docs/deferred-queue.md`.
- Step 5: `src/engine/character.ts` (model, validator, JSON export/import), `saves.ts` (`CharacterStore` over a `KeyValueStore`), `idbStore.ts` (idb-keyval adapter). 12 tests in `tests/character.test.ts` tagged `[s1]`. Characters store only pack entry ids. Schema version 1; no migrations yet.
- Step 4: `schema/source-pack.schema.json` (ajv-validated), `src/engine/packs.ts` (`validatePack`, `loadPacks`: extends ordering, duplicate-id errors, id-preserving overrides), 13 tests in `tests/packs.test.ts` tagged `[s1]`. Per-kind entry fields (beyond id/name/description) are deliberately open until the SRD shape is known in Series 2.
- Step 3: harness. `Run-All-Tests.bat` (or `node scripts/run-all-tests.mjs`) syncs `TestPolicy/test-master-list.csv`, runs vitest, writes `TestArtifacts/<run-id>/report.html`. Governance = top-level `describe` name must start with `[evergreen]` or `[sN]`; sync fails otherwise. Flags applied by `tests/setup.ts` (0 = skip). `npm test` also honors the list.
- Step 2: Vite/React/TS scaffold, vitest (`npm test`), oxlint (`npm run lint`), folders `src/engine`, `src/ui`, `schema`, `packs`, `tests`. Build, smoke test, lint pass.
- Collaboration scaffolding (CLAUDE.md, backlog, plan, deferred queue, .gitignore, TestPolicy list, memory).
- Series 1-6 drafted in `Docs/plan.md`.

## Unverified
- New page 2 layout (inventory top, rules bottom, notes space between) has been rendered and passes margin/grayscale checks but nobody has looked at it. The page 1 spill (Brawler stances, Druid, multiclass modules) is not yet fixed; options were put to the owner.
- Pattern audit checks margins, grayscale, page counts and rules, not looks: where the extra pages come from (card pages vs page 1 continuation) is not broken down, and no one has viewed the 6-page sheets.
- Level N sheets: only checked by numbers, text and margins; no one has looked at the sample PDFs (layout, column balance, page count with multiclass + all subclass cards). Not printed: multiclass module extras when the added class has a companion, stances or beastforms (only its class features print); Ranger "Light in the Dark" extra Hope slot (Hope stays 6 boxes); Brawler Combo Die shown as text only. Version list touch-up is on the long-term list.
- Level-up UI (owner-verified 2026-09-22): owner previewed a new character through the level-up flow live, including the new class step (starting stances / starting companion for a multiclass). Looks right. Level 2+ characters open a summary screen, not the creation wizard, and cannot yet Make PDF (step 4). Character notes/gear edits after leveling are not in the UI yet.
- Level-up engine (owner-confirmed 2026-09-20): slots are marked in a tier box at or below the current tier (usually the current tier). Multiclass crosses out one unused upgraded-subclass option anywhere on the sheet (default: its own tier's; `crossOutTier` picks another) plus all other Multiclass options; taking an upgraded subclass card crosses out that tier's Multiclass box (so it cannot be taken where Multiclass was). Extra domain card cap = the character's level (no tier cap). Swap: same level or lower, only cards held before this level. New and starting Experiences are +2. Ranger companion options: 1 per level, plus the extras from Expert Training (+1) / Advanced Training (+2) once, on the level that feature is taken; each option once. Subclass upgrade may target the multiclass subclass. Multiclass domain cards: at or below half level rounded up. Not modelled: loadout vs vault placement, HP/Stress slot maximums (sheet rows cap at 12).
- Level-up engine is not used by the UI, `deriveStats` or the PDF yet: they still read level 1 data (`choices.domainCardIds` and `traits` stay the level 1 base; totals come from `progressOf`). `validateCreation` is level 1 only.
- Level-up data: Brawler Combo Die is modelled as an advancement (1 slot per tier, costs 1 of the 2 advancements) from the SRD phrase "as a level advancement option"; the SRD gives no slot or cost, owner to confirm. Ranger extra companion options (Expert/Advanced Training) are subclass features, handled in step 2. The multiclass "half level" card rule is text only until step 2. The older `[s3]` class-extras multiclass test was skipped by policy this session; the same fact is asserted in the new `[s5]` test.
- PDF: page 1 of the wizard sample was looked at in the browser pane (matches the mockup); other classes/pages not.  only page counts and no-crash checked. Spacing, overlaps, and grayscale print not judged. Not covered: Druid beastforms (not printed), frame-attached trackers other than weapons (Tech upgrade slots print in the Trackers block), non-latin characters (replaced by `?`), Make PDF button not clicked in the browser.
- UI: verified by driving it in the browser pane (one path). Not tried: other classes' flows, dark mode, phone width, print. IndexedDB works (autosave, reload, reopen confirmed). Owner has not looked at the visuals; styling is a plain placeholder.
- Rules gaps: armor features that change numbers (e.g. "+1 to Evasion") are shown as text only, not added into Evasion/thresholds. Ranger companion Stress slots are not in the SRD text I parsed, so not modelled. Legendary/Mythic Beast entries are upgrade rules (no stat line) and keep their text as features. "Once per rest" detection is a text match on rules; the 65 cards were counted, not read one by one. Mixed Ancestry, Beastbound and Martial Artist paths are covered by engine tests, Beastbound also by the live run.
- Creation engine: Tech's replace-pool points at a builder choice, so its pool has no entries (the builder is the pool; wizard must handle). Conflicts are reported, not resolved: the start screen (step 4) decides how to block or warn. Frame ops other than pools/steps/annotate are passed through in `ops` for later steps.
- Frame pack judgment calls (owner confirmed 12 Wither boxes, 6 scars, Prepare removed, Tech slots): Wither tracker is 12 boxes (Fear Die is a d12; SRD gives no per-PC count); Grimdark scars tracker is 6 boxes (Hope slots); Feasts removes Tend to Wounds, Clear Stress and Prepare (owner: Feast replaces gaining Hope; note Prepare also refreshes per-rest uses, which the wizard/back sheet must still cover); Tech Upgrade slots are 2 + 1 per tier (`perTier`). Colossal, Hex Crawl and Faction Tracking are GM-only and left out. Conflict detection between supplements is not implemented (wizard work).
- Core pack: generated by script, checked by counts/refs only. Rules text is verbatim SRD but not compared line by line. Mixed Ancestry (choice type `pick-one-each-from`) and Multiclassing are not yet modelled. Ranger companion, Druid beastforms and Brawler stances sit in a class `supplements` list as raw rules text, not structured.
- `idbStore.ts` (real IndexedDB) untested until a UI exists.
- Harness verified from the CLI (run, skip, ungoverned failure); browser auto-open fixed (path escape bug) and confirmed by owner opening the report.
- SRD Markdown files: checked by counts and link checks, not read line by line. Pages checked against images: 101, 102 (hand-transcribed), 193, 194, 196. Details in `Daggerheart SRD Files/Conversion Notes.md`.

## Condensed wording process (for a fresh session)
- SRD text stays in `rules`; sheet text is `short` in `scripts/srd-convert/short/<classId>.json` (see `short-text.mjs` header for the format; arrays align by position, names are checked, `""` folds a feature into the one above). Creator shows `rules`; sheet prints `short ?? rules`.
- Style (owner approved on Assassin): keep names, game terms, numbers and every mechanical effect; cut examples, filler and "you can"; fold the SRD's split paragraphs into one feature; keep lists as one compact sentence; never add numbers that are not in the SRD text; unnamed features that begin "Name." keep that lead in the short text.
- Loop per class: print the class with `node scripts/show-class.cjs <classId>`-style script (class hope, features, subclass features, stances, companion options from `packs/core/srd-core.json`), write the JSON, then `node scripts/srd-convert/build-core-pack.mjs`, `npx vitest run tests/short-text.test.ts`, `node scripts/short-report.mjs`, send `TestArtifacts/short-review/<class>.md` to the owner, wait for approval.
- Owner decisions: Brawler stances, Assassin poisons and the Druid elements table print on the domain card sheet before the cards (only known/chosen; see short-text.mjs `cards` / `table`); Druid Beastform definitions say "see SRD" (forms page is on the long-term list); Ranger companion options also get shorts; batches of about four classes per review are fine.

- Vitality (Blade domain card, lvl 5): owner clarified it's a one-time permanent choice like a mini level-up — pick 2 of {Stress slot, Hit Point slot, +2 damage thresholds} the level the card is taken, then fixed forever (the card being "vaulted permanently" doesn't matter since loadout/vault isn't modelled). Not a footnote candidate (nothing conditional survives after the pick). Built: `LevelRecord.vitalityChoice` (schema, `character.ts`), `Progress.vitalityThresholds` + validation in `applyRecord` (`levelup.ts`: exactly 2 of the 3, room-checked against the 12 cap like the HP/Stress advancements, errors if a choice is given on a level that didn't take the card), baked into `sheetView.ts` thresholds. UI: `VitalityPicker` in `LevelUp.tsx`, shown on the cards step (new card or swap-in) and the advancements step (if taken as the extra-card advancement); review step summarizes the choice. Tests in `tests/stat-notes.test.ts` (2 new) and `tests/lib/patterns.ts` updated (the level-up pattern generator now supplies a Vitality choice when it randomly picks the card, needed for `tests/level-patterns.test.ts` to keep passing). Full suite 41 passed, 0 failed, 151 skipped. Not checked live in the browser UI.

- Owner correction 2026-09-23: after checking a printed sheet (Warrior with Fortified Armor), the † "build" bonuses that depend only on the character's own equipped gear (not loadout composition) are now computed straight into the printed number, not left as a footnote — owner: "assume our displayed armor and weapons are equipped." Moved out of `stat-notes.mjs` STAT_NOTES into `sheetView.ts` gear-conditional math (`wornArmor`/`gearBonus`, computed from `ch.choices.equipmentIds` and held domain cards): Armorer (+1 Armor Score while wearing armor), Fortified Armor (+2 both thresholds while wearing armor), Mage Robes x4 (threshold bonus = Spellcast trait, using the higher of two if multiclassed), Granminster's Finery (Armor Score bonus = Presence). Footnotes now remain only for: genuinely situational (‡) bonuses that depend on live play state we don't track (Scorpion's Poise, Battle-Bonded, Elusive Predator, Shadowhunter, Eldritch Flesh, Vial of Darksmoke) and the loadout-dependent -Touched cards (still a footnote — loadout vs. vault is never modelled, per the 2026-09-22 decision). Tests updated/added in `tests/stat-notes.test.ts` (9, was 7); one existing test's expected math had to account for a stalwart-to-mastery test character organically holding Fortified Armor from its auto-picked level cards. Full suite 43 passed, 0 failed, 151 skipped.

- Series 8 step 1, Druid forms page (owner-approved 2026-09-23): all 24 Beastforms print on their own page after the domain cards, Druid main class only, filtered to the character's tier or lower. Two columns, one zebra-striped row per form. Header: name left, "Ev. N" centered, "Th major/severe" right-aligned. Trait line: all six traits as abbreviated codes (AGI/STR/FIN/INS/PRE/KNO) with modifier signs, the form's boosted trait bold. "Atk. range, mod, XdY phy/mag" and "Adv. list" share a row, Atk. at the block's left edge, Adv. at the block's own center. Legendary Beast / Mythic Beast (upgrade templates, no fixed stats) print as name + text only. Data: `BeastformView` in `sheetView.ts` (trait total, Evasion, thresholds computed against the character's own sheet — Proficiency dice via the existing `damageText`); `thresholdBonus` added to the beastform schema and parsed in `class-extras.mjs` (regex fixed to also catch "bonus to **all** your damage thresholds" — this is what surfaced Massive Behemoth's Undaunted, missed on the first pass). Feature wording condensed via the existing short-text pipeline (`short/druid.json` `beastforms` key, `short-text.mjs`, checked by `tests/short-text.test.ts`: no new numbers, never longer than SRD) — needed to fit 24 forms in 2 pages instead of 3-4 with SRD-verbatim text. Tests: `tests/beastforms.test.ts` (4, `[s8]`). Full suite 47 passed, 0 failed, 151 skipped. Owner reviewed 3 sample-PDF rounds live (stats math, then layout/abbreviations, then condensed text) and approved the final 2-page result "with room to spare."

## Very next action
- Series 7 step 1 and Series 8 step 1 are both closed out and owner-approved. Nothing blocking. Candidates for next: Series 7 step 2 (condense class/subclass/rules text for the level-10 <= 4-page target — the same short-text pipeline used for the Beastform page applies here), Series 6 (custom sources, untouched), or another item from `long-term-to-do.md`.
