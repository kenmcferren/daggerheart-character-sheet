# Series 2: Primary book data
Status: complete
1. [x] Get the SRD text and decide the conversion workflow: PDF split into Markdown in `Daggerheart SRD Files/`; scripts in `scripts/srd-convert/`
2. [x] Pack shape: apply `Docs/campaign-frame-impacts.md` (choice types, frame ops, trackers) to the schema, then build the core rulebook pack: ancestries, communities, classes, subclasses, domains, domain cards, equipment
3. [x] Campaign frame pack(s) from the second primary book
4. [x] Pack validation report (counts, missing references)

Deliverables: `Daggerheart SRD Files/`, `packs/core/srd-core.json`, `packs/core/srd-frames.json`, `scripts/srd-convert/`, `scripts/validate-packs.mjs`, `Docs/campaign-frame-impacts.md`. Tests tagged `[s2]` set to skip.
